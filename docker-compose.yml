version: "3.8"

services:
  spring:
    build: ./bowlingmate
    container_name: bowling-spring
    ports:
      - "8080:8080"
    depends_on:
      - flask
    environment:
      - FLASK_URL=http://flask:5000
    volumes:
      - ./shared:/app/shared   # 결과 공유 폴더

  flask:
    build: ./ai_server
    container_name: bowling-flask
    ports:
      - "5000:5000"
    volumes:
      - ./shared:/app/shared